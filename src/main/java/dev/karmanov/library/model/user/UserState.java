package dev.karmanov.library.model.user;

public enum UserState {
    DEFAULT,
    AWAITING_INPUT,
    AWAITING_TEXT,
    AWAITING_CALLBACK,
    AWAITING_VOICE,
    AWAITING_PHOTO,
    AWAITING_MEDIA,
    AWAITING_DOCUMENT,
    AWAITING_CONTACT,
    AWAITING_LOCATION,
    AWAITING_COMMAND,
    AWAITING_CONFIRMATION,
    AWAITING_SELECTION,
    AWAITING_MULTISTEP_INPUT,
    AWAITING_ROLE_SELECTION
}

